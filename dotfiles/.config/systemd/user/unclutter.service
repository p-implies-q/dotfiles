[Unit]
Description = unclutter, to hide the mouse cursor after x seconds idle
PartOf = xsession.target

[Service]
Type = simple
ExecStart = /usr/bin/unclutter -idle 0.5

[Install]
WantedBy = xsession.target
